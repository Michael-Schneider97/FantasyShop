/*
 * Handles the simulation
 * Manages time
 */￼
 
 #include "ItemMasterList.h"
 
 enum class worldState {pause, resume};
 
 class World 
 {
	 public:
	 // call this before doing stuff
	 bool init();
	 // starts the simulation(?)
	 void execute(); 
	 
	 /*
	  * Once theyve been designed
	  * player, store, npcs will 
	  * be in the data section below
	  */
	  
	 
 
 };